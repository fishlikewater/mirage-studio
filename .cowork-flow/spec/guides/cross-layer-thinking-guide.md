# 跨层思考指引

## 什么时候必须看

- 变更穿过 3 层及以上
- 接口、状态、权限、异步链路发生变化
- 一个修改需要同步多个模块或多个进程

## 检查问题

- 输入从哪里来？经过哪些层？最终落到哪里？
- 输出给谁？哪些层依赖它？
- 错误在每一层如何表达和传播？
- 权限、鉴权、业务授权是否被混淆？
- 是否只改了主路径，漏掉异常路径和回滚路径？

## 常见漏项

- Controller 改了，但 service / repository / client 没改
- 消息结构改了，但消费者没同步
- 状态机规则改了，但校验和展示没同步
