# 编码前检查表

## 在动手前先确认

- 这次改动属于 `L0`、`L1` 还是 `L2`？
- 是否涉及 API、数据库、异步链路、权限或状态流转？
- 是否需要先补 `cowork-flow change`？
- 是否已经明确成功标准和验证方式？
- 是否已经搜索过相似实现与可复用能力？

## 如果涉及行为变化

- 是否已有 `proposal/spec/design/plan`？
- 是否已经明确输入、输出、错误、边界场景？
- 是否知道谁负责提交业务代码？
