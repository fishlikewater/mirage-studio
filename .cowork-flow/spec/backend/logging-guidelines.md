# 后端日志规范

## 目标

- 便于排查
- 便于追踪链路
- 不泄露敏感信息

## 推荐做法

- 使用结构化日志
- 统一记录请求标识、用户标识、任务标识或链路标识
- 错误日志记录上下文，不重复打印同一异常
- 对账号、令牌、密钥、身份证号、手机号等敏感字段脱敏

## 不建议

- 直接输出完整请求体或完整响应体
- 使用临时 `print` / `console` 式调试输出长期留在主干
- 同一错误在多层重复打印
