# 后端质量规范

## 完成定义

至少满足：

- 编译 / 构建通过
- 测试通过
- 核心场景已验证
- 新增约定已沉淀到 spec

## 推荐门禁

- 静态检查：`<按项目填写>`
- 单元测试：`<按项目填写>`
- 集成测试：`<按项目填写>`
- 构建验证：`<按项目填写>`

## 常见禁止模式

- 未验证就宣称完成
- 用硬编码绕过配置与契约
- 用临时兼容逻辑代替真实修复
- 变更接口或数据结构却不更新文档和调用方
