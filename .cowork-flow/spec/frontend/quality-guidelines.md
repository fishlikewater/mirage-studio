# 前端质量规范

## 完成定义

至少满足：

- 构建通过
- 类型检查通过
- lint / format 通过
- 关键交互已手工验证

## 推荐门禁

- lint：`<按项目填写>`
- type-check：`<按项目填写>`
- test：`<按项目填写>`
- build：`<按项目填写>`

## 常见禁止模式

- 用 `any` 或强制断言掩盖真实类型问题
- 组件未处理加载、空态、错误态
- 交互依赖隐式副作用
